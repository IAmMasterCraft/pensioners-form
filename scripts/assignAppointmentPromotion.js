//post
const post = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Post",
});

//appointmentPromotionDate
const appointmentPromotionDate = formInput({
    inputSize: "col-4",
    inputType: "date",
    name: "Date Of Appointment/Promotion",
});

//scaleOfSalary
const scaleOfSalary = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Scale Of Salary",
});

//scaleEntryPoint
const scaleEntryPoint = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Entry Point on the Scale",
});

//incrementalDate
const incrementalDate = formInput({
    inputSize: "col-4",
    inputType: "date",
    name: "Incremental Date",
});

//deferredIncrement
const deferredIncrement = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Increments deferred or withheld",
});

//reasonForChange
const reasonForChange = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Reason For Change",
});

//authority
const authority = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Authority",
});