//actingPost
const actingPost = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Acting Post",
});

//actingFrom
const actingFrom = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Acting From",
});

//actingTo
const actingTo = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Acting To",
});

//gazetteReference
const gazetteReference = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Gazette Reference",
});

//allowanceRate
const allowanceRate = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "allowanceRate",
});