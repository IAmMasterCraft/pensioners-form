//fromSickLeave
const fromSickLeave = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "From",
});

//toSickLeave
const toSickLeave = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "To",
});

//totalSickLeave
const totalSickLeave = formInput({
    inputSize: "col-4",
    inputType: "text",
    name: "Total Month & Days",
    placeHolder: "Enter total month & days for Sick Leave",
});